import styled from "styled-components";

export const Background = styled.div`
    display: flex;

    position: absolute;
    top: 0; 
    bottom: 0;
    left: 0; 
    right: 0;

    width: 50vw;

    .user-icon {
        /* width: 80%; */
        padding-left: 8rem;
    }
`